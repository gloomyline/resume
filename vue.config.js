module.exports = {
    devServer: {
        // Tells dev-server to open the browser after server had been started.
        // Set it to `true` to open the default browser.
        open: true,
        port: 8888
    }
}
